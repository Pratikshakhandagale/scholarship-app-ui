import React from "react";

function Loading() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100vh",
      }}
    >
      <h2>🌀 Loading...</h2>
    </div>
  );
}

export default Loading;
